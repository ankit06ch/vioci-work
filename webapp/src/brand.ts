/** Full wordmark (icon + text) — sidebar expanded. */
export const VIOCI_LOGO_SRC = '/vioci-light.png'

/** Icon-only mark (no wordmark) — sidebar rail, favicon, auth. */
export const VIOCI_ICON_SRC = '/vioci-icon.png'
